"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from datetime import datetime
from polar_sdk.types import BaseModel, Nullable, OptionalNullable, UNSET, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Dict, Optional, Union
from typing_extensions import NotRequired, TypeAliasType, TypedDict


OrderCreateCustomFieldDataTypedDict = TypeAliasType(
    "OrderCreateCustomFieldDataTypedDict", Union[str, int, bool, datetime]
)


OrderCreateCustomFieldData = TypeAliasType(
    "OrderCreateCustomFieldData", Union[str, int, bool, datetime]
)


OrderCreateMetadataTypedDict = TypeAliasType(
    "OrderCreateMetadataTypedDict", Union[str, int, float, bool]
)


OrderCreateMetadata = TypeAliasType("OrderCreateMetadata", Union[str, int, float, bool])


class OrderCreateTypedDict(TypedDict):
    r"""Schema to create a draft order for an off-session charge."""

    customer_id: str
    r"""The ID of the customer the order is for. Must belong to the order's organization."""
    product_id: str
    r"""The ID of the one-time product to charge for. Must belong to the order's organization. Only fixed-price and free products are supported."""
    custom_field_data: NotRequired[
        Dict[str, Nullable[OrderCreateCustomFieldDataTypedDict]]
    ]
    r"""Key-value object storing custom field values."""
    metadata: NotRequired[Dict[str, OrderCreateMetadataTypedDict]]
    r"""Key-value object allowing you to store additional information.

    The key must be a string with a maximum length of **40 characters**.
    The value must be either:

    * A string with a maximum length of **500 characters**
    * An integer
    * A floating-point number
    * A boolean

    You can store up to **50 key-value pairs**.
    """
    organization_id: NotRequired[Nullable[str]]
    r"""The ID of the organization the order belongs to. **Required unless you use an organization token.** The customer and product must belong to this organization."""
    currency: NotRequired[Nullable[str]]
    r"""The currency to charge in (ISO 4217, lowercase, e.g. `usd`). Defaults to the organization's default currency; specify it to force a different one, or when the product isn't priced in the organization's default currency."""
    amount: NotRequired[Nullable[int]]
    r"""A custom amount to charge, in the smallest currency unit. Overrides the product's price; defaults to the product's configured price (0 for free products). A positive amount must be at least the currency's minimum."""
    description: NotRequired[Nullable[str]]
    r"""A custom description for the order's line item, shown on the invoice and receipt (e.g. `5,000 tokens`). Defaults to the product name."""


class OrderCreate(BaseModel):
    r"""Schema to create a draft order for an off-session charge."""

    customer_id: str
    r"""The ID of the customer the order is for. Must belong to the order's organization."""

    product_id: str
    r"""The ID of the one-time product to charge for. Must belong to the order's organization. Only fixed-price and free products are supported."""

    custom_field_data: Optional[Dict[str, Nullable[OrderCreateCustomFieldData]]] = None
    r"""Key-value object storing custom field values."""

    metadata: Optional[Dict[str, OrderCreateMetadata]] = None
    r"""Key-value object allowing you to store additional information.

    The key must be a string with a maximum length of **40 characters**.
    The value must be either:

    * A string with a maximum length of **500 characters**
    * An integer
    * A floating-point number
    * A boolean

    You can store up to **50 key-value pairs**.
    """

    organization_id: OptionalNullable[str] = UNSET
    r"""The ID of the organization the order belongs to. **Required unless you use an organization token.** The customer and product must belong to this organization."""

    currency: OptionalNullable[str] = UNSET
    r"""The currency to charge in (ISO 4217, lowercase, e.g. `usd`). Defaults to the organization's default currency; specify it to force a different one, or when the product isn't priced in the organization's default currency."""

    amount: OptionalNullable[int] = UNSET
    r"""A custom amount to charge, in the smallest currency unit. Overrides the product's price; defaults to the product's configured price (0 for free products). A positive amount must be at least the currency's minimum."""

    description: OptionalNullable[str] = UNSET
    r"""A custom description for the order's line item, shown on the invoice and receipt (e.g. `5,000 tokens`). Defaults to the product name."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = [
            "custom_field_data",
            "metadata",
            "organization_id",
            "currency",
            "amount",
            "description",
        ]
        nullable_fields = ["organization_id", "currency", "amount", "description"]
        null_default_fields = []

        serialized = handler(self)

        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)
            serialized.pop(k, None)

            optional_nullable = k in optional_fields and k in nullable_fields
            is_set = (
                self.__pydantic_fields_set__.intersection({n})
                or k in null_default_fields
            )  # pylint: disable=no-member

            if val is not None and val != UNSET_SENTINEL:
                m[k] = val
            elif val != UNSET_SENTINEL and (
                not k in optional_fields or (optional_nullable and is_set)
            ):
                m[k] = val

        return m
