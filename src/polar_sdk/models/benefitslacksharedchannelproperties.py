"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from polar_sdk.types import BaseModel, Nullable, OptionalNullable, UNSET, UNSET_SENTINEL
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict


class BenefitSlackSharedChannelPropertiesTypedDict(TypedDict):
    slack_integration_id: str
    r"""Polar Slack integration linked to this benefit."""
    channel_name_template: str
    r"""Template for the channel name. Supports placeholders: {customer_name}, {customer_email_local}, and {metadata.<key>} for any value stored in customer user metadata."""
    private: NotRequired[bool]
    r"""Create the channel as private (recommended)."""
    welcome_message: NotRequired[Nullable[str]]
    r"""Optional message posted to the channel right after creation."""
    archive_on_revoke: NotRequired[bool]
    r"""Archive the channel when the benefit is revoked."""
    team_invitees: NotRequired[List[str]]
    r"""Slack user IDs from the merchant workspace to invite to every channel created for this benefit."""


class BenefitSlackSharedChannelProperties(BaseModel):
    slack_integration_id: str
    r"""Polar Slack integration linked to this benefit."""

    channel_name_template: str
    r"""Template for the channel name. Supports placeholders: {customer_name}, {customer_email_local}, and {metadata.<key>} for any value stored in customer user metadata."""

    private: Optional[bool] = True
    r"""Create the channel as private (recommended)."""

    welcome_message: OptionalNullable[str] = UNSET
    r"""Optional message posted to the channel right after creation."""

    archive_on_revoke: Optional[bool] = True
    r"""Archive the channel when the benefit is revoked."""

    team_invitees: Optional[List[str]] = None
    r"""Slack user IDs from the merchant workspace to invite to every channel created for this benefit."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = [
            "private",
            "welcome_message",
            "archive_on_revoke",
            "team_invitees",
        ]
        nullable_fields = ["welcome_message"]
        null_default_fields = []

        serialized = handler(self)

        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)
            serialized.pop(k, None)

            optional_nullable = k in optional_fields and k in nullable_fields
            is_set = (
                self.__pydantic_fields_set__.intersection({n})
                or k in null_default_fields
            )  # pylint: disable=no-member

            if val is not None and val != UNSET_SENTINEL:
                m[k] = val
            elif val != UNSET_SENTINEL and (
                not k in optional_fields or (optional_nullable and is_set)
            ):
                m[k] = val

        return m
