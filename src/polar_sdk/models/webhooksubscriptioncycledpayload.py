"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .subscription import Subscription, SubscriptionTypedDict
from datetime import datetime
from polar_sdk.types import BaseModel
from polar_sdk.utils import validate_const
import pydantic
from pydantic.functional_validators import AfterValidator
from typing import Literal
from typing_extensions import Annotated, TypedDict


class WebhookSubscriptionCycledPayloadTypedDict(TypedDict):
    r"""Sent when a subscription enters a new billing period.

    The payload carries the new `current_period_start` and `current_period_end`.
    It fires when the period rolls over, before the renewal order exists and
    regardless of whether the renewal payment succeeds — listen to `order.paid`
    if you need the payment.

    A trial converting to a paid subscription starts a new period, so it fires
    there too. Read `status` to tell the two apart.

    **Discord & Slack support:** Basic
    """

    timestamp: datetime
    data: SubscriptionTypedDict
    type: Literal["subscription.cycled"]


class WebhookSubscriptionCycledPayload(BaseModel):
    r"""Sent when a subscription enters a new billing period.

    The payload carries the new `current_period_start` and `current_period_end`.
    It fires when the period rolls over, before the renewal order exists and
    regardless of whether the renewal payment succeeds — listen to `order.paid`
    if you need the payment.

    A trial converting to a paid subscription starts a new period, so it fires
    there too. Read `status` to tell the two apart.

    **Discord & Slack support:** Basic
    """

    timestamp: datetime

    data: Subscription

    TYPE: Annotated[
        Annotated[
            Literal["subscription.cycled"],
            AfterValidator(validate_const("subscription.cycled")),
        ],
        pydantic.Field(alias="type"),
    ] = "subscription.cycled"
