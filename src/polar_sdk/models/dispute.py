"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .disputecustomer import DisputeCustomer, DisputeCustomerTypedDict
from .disputestatus import DisputeStatus
from datetime import datetime
from polar_sdk.types import BaseModel, Nullable, UNSET_SENTINEL
from pydantic import model_serializer
from typing_extensions import TypedDict


class DisputeTypedDict(TypedDict):
    r"""Schema representing a dispute.

    A dispute is a challenge raised by a customer or their bank regarding a payment.
    """

    created_at: datetime
    r"""Creation timestamp of the object."""
    modified_at: Nullable[datetime]
    r"""Last modification timestamp of the object."""
    id: str
    r"""The ID of the object."""
    status: DisputeStatus
    resolved: bool
    r"""Whether the dispute has been resolved (won or lost)."""
    closed: bool
    r"""Whether the dispute is closed (prevented, won, or lost)."""
    amount: int
    r"""Amount in cents disputed."""
    tax_amount: int
    r"""Tax amount in cents disputed."""
    currency: str
    r"""Currency code of the dispute."""
    reason: Nullable[str]
    r"""The reason for the dispute as reported by the card network (e.g. `fraudulent`, `product_not_received`). `None` until the processor reports it."""
    evidence_due_by: Nullable[datetime]
    r"""Deadline to submit evidence in response to the dispute. `None` when no response is required."""
    past_due: bool
    r"""Whether the evidence submission deadline has passed."""
    order_id: str
    r"""The ID of the order associated with the dispute."""
    payment_id: str
    r"""The ID of the payment associated with the dispute."""
    customer: DisputeCustomerTypedDict
    case_id: Nullable[str]
    r"""The ID of the support case for this dispute, if one was opened."""


class Dispute(BaseModel):
    r"""Schema representing a dispute.

    A dispute is a challenge raised by a customer or their bank regarding a payment.
    """

    created_at: datetime
    r"""Creation timestamp of the object."""

    modified_at: Nullable[datetime]
    r"""Last modification timestamp of the object."""

    id: str
    r"""The ID of the object."""

    status: DisputeStatus

    resolved: bool
    r"""Whether the dispute has been resolved (won or lost)."""

    closed: bool
    r"""Whether the dispute is closed (prevented, won, or lost)."""

    amount: int
    r"""Amount in cents disputed."""

    tax_amount: int
    r"""Tax amount in cents disputed."""

    currency: str
    r"""Currency code of the dispute."""

    reason: Nullable[str]
    r"""The reason for the dispute as reported by the card network (e.g. `fraudulent`, `product_not_received`). `None` until the processor reports it."""

    evidence_due_by: Nullable[datetime]
    r"""Deadline to submit evidence in response to the dispute. `None` when no response is required."""

    past_due: bool
    r"""Whether the evidence submission deadline has passed."""

    order_id: str
    r"""The ID of the order associated with the dispute."""

    payment_id: str
    r"""The ID of the payment associated with the dispute."""

    customer: DisputeCustomer

    case_id: Nullable[str]
    r"""The ID of the support case for this dispute, if one was opened."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = []
        nullable_fields = ["modified_at", "reason", "evidence_due_by", "case_id"]
        null_default_fields = []

        serialized = handler(self)

        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)
            serialized.pop(k, None)

            optional_nullable = k in optional_fields and k in nullable_fields
            is_set = (
                self.__pydantic_fields_set__.intersection({n})
                or k in null_default_fields
            )  # pylint: disable=no-member

            if val is not None and val != UNSET_SENTINEL:
                m[k] = val
            elif val != UNSET_SENTINEL and (
                not k in optional_fields or (optional_nullable and is_set)
            ):
                m[k] = val

        return m
